; Maximum number of Samus tiles in PPU for the "Samus on the fly". Valid options: 16, 32, 64
CFG_NUM_SAMUS_TILES = 64

; 0 = beam items respawn (vanilla), 1 = don't
CFG_BEAM_UNIQUE_ITEMS = 0

; 0 = don't stack (vanilla), 1 = stack
CFG_BEAM_STACK = 1

; 0 = 1 missile at a time (vanilla), 1 = multiple at a time
CFG_UNCAPPED_MISSILES = 0

; 0 = use passwords, 1 = use saves
CFG_SAVE = 1

; 0 = mirror right-facing frames, 1 = use unique left-facing frames
; TODO: only works when CFG_NUM_SAMUS_TILES == 64
CFG_UNIQUE_LEFT_FACING_SAMUS_FRAMES = 1
