; Maximum number of Samus tiles in PPU for the "Samus on the fly". Valid options: 16, 32
CFG_NUM_SAMUS_TILES = 16

; 0 = beam items respawn, 1 = don't
CFG_BEAM_UNIQUE_ITEMS = 0

; 0 = don't stack, 1 = stack
CFG_BEAM_STACK = 0
